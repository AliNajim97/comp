name: emp1
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  iconsax: ^0.0.8
  simple_animations: ^4.0.1
  dialog: ^0.8.0
  google_nav_bar: ^5.0.6
  splashscreen: ^1.3.5
  flutter_launcher_icons: ^0.9.0
  flare_flutter: ^3.0.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "img/logo.png"
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
flutter:

  uses-material-design: true

  assets:
     - img/logo.png

  fonts:
     - family: cr
       fonts:
         - asset: img/CAREEM-REGULAR..otf
  #         style: italic


